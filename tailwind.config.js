const colors = require('windicss/colors')
const typography = require('windicss/plugin/typography')


module.exports = {
  darkMode: 'media',  
  theme: {
      fontFamily: {
        sans: ['Lato', 'sans'],
      }
    }
}